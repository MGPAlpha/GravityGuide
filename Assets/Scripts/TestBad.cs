using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestBad : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        This Shouldn't Compile
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
